"use server";

/**
 * User Actions
 *
 * Server actions for user-related operations
 *
 * Note: getUserSettings is exported from settings.ts to avoid duplication
 */

// Currently empty - add user-specific actions here as needed
// getUserSettings is available in settings.ts
